msrv = "1.75"
cognitive-complexity-threshold = 30
too-many-arguments-threshold = 8
# Arbitrary-precision integers cannot overflow or wrap; their only failure is
# allocation, which aborts. Primitive arithmetic stays denied.
arithmetic-side-effects-allowed = ["num_bigint::BigInt", "num_bigint::BigUint"]
