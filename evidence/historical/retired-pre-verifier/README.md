# Retired pre-verifier records

These records predate independent outcome re-derivation and a committed record-set anchor. They are
retained as non-authoritative history and cannot support a current assurance claim. Their complete
tree is recursively bound by `evidence/ANCHORS`.
